#include <stdio.h>
#include <pthread.h>

void run_func(void *arg) {
    // print numbers 0-5, prefixed by thread name
}

int main(void) {
    // have main thread spawn 2 other threads
    // pthread_create w thread name as fourth argument
    // pthread_join to exit
}

